# GNews API key (free: https://gnews.io/)
GNEWS_API_KEY=your_api_key_here

# Database URL (SQLite, no external service needed)
DATABASE_URL="file:./dev.db"

# Enable dev fetch endpoint (set true only in dev)
ENABLE_DEV_FETCH=true
